# This is the structure of the classes from the reports "Structure" section

import State
import Action

class Pattern:
  
  def __init__(self, states, action):
    self.states = states
    self.action = action
